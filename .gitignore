
# local preview helpers (do not deploy)
.start-preview.ps1
.stop-preview.ps1
.playwright-mcp/

# orphaned images kept locally only (38MB, not deployed)
PNG/_unused/

# pnpm lint 的暫存檔，正常跑完會自己刪，中途掛掉才會留下
.tw-check.css
.syntax-check.mjs
